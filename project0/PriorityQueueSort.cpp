#include "PriorityQueueSort.h"
namespace ods{
  
}

  /*
  template<class T>
  void PriorityQueueSort<T>::sorting(int size, int index)
  {
    for(int counter=0; counter <=ArrayQueue<T>::n-1; counter++)
  {
        for(int index=0; index <ArrayQueue<T>::n-1; index++)
      {
          int temp;
          if(ArrayQueue<T>::a[index]>ArrayQueue<T>::a[index+1])
          {
              temp=ArrayQueue<T>::a[index];
              ArrayQueue<T>::a[index]=ArrayQueue<T>::a[index+1];
              ArrayQueue<T>::a[index+1]=temp;
          }
      }
    }
  }*/

/*void PriorityQueueSort<T>::display()
{
  for(int i=0; i<n;i++)
  cout<<a[i]<<" ";
}
*/
